{
  "compilerOptions": {
    "baseUrl": ".", // 기본 경로 설정
    "paths": {
      "ServerCore/*": ["../ServerCore/*"], // ServerCore를 절대 경로로 설정
      "src/*": ["./src/*"] // src도 절대 경로로 설정
    }
  },
  "include": [
    "src/**/*", // src 내부 파일 포함
    "../ServerCore/**/*" // ServerCore 파일 포함
  ],
  "exclude": ["node_modules"]
}
